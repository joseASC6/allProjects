/*function randomNum(num){
    let number = Math.random() * (num - 0) + 0
    number = Math.floor(number);
    console.log(number);
}

randomNum(100); 

function randomLet(){
    var blah = ["B", "L", "A", "H"]
    let number = Math.random() * (4 - 0) + 0;
    let round = Math.floor(number);
    console.log[number]
}

function randomLet(string){
    let max = string.length
    let interval = Math.random() * (max - 0) + 0;
    interval = Math.floor(interval);
    console.log(sting[interval]);
}
randomLet('blahopasfg")

randomLet();
let word = "abcdefg"
let number = Math.random() * (7 - -1) + -1;
let round = Math.floor(number);
console.log(word[round]);

let phrase = ["take it easy"]
phrase.push('my guy');
console.log(phrase); 
function hypontenuse(sideA, sideB){
    let sideA_squared = Math.pow(sideA, 2);
    let sideB_squared = Math.pow(sideB, 2);
    let sideC_Sqaured = sideA_squared + sideB_squared;
    let sideC = Math.sqrt(sideC_Sqaured, 2);
    console.log(sideC);
}
hypontenuse(5, 12);*/
function lunch(number){
    return number;
    //console.log(number);
}
if(lunch(70) == 70){
    console.log("They are equal");
}